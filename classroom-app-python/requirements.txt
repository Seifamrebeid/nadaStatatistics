opencv-python==4.10.0.84
# face_recognition + dlib are installed separately by the setup script (see README.md)
# because face_recognition==1.3.0 hard-requires `dlib` (source-only on Windows) and we
# substitute `dlib-bin` (prebuilt wheel). Installing face_recognition here with
# --no-deps is the workaround; keeping them out of this file avoids pip re-resolving.
fer==22.5.1
moviepy==1.0.3               # fer 22.5.1 expects moviepy v1.x (`moviepy.editor` submodule); v2 removed it
tensorflow==2.15.0
mediapipe==0.10.14
sounddevice==0.4.6
scipy==1.13.1
faster-whisper==1.0.3
silero-vad==5.1
torch==2.3.1
torchaudio==2.3.1
numpy==1.26.4
firebase-admin==6.5.0
requests==2.32.3
python-dotenv==1.0.1
ultralytics>=8.4              # YOLOv8 nano — cell phone detection for "on phone" warning
